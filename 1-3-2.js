// De(out, 반대) + structur + ing

const person = {
  name: "JENNIE",
  age: "27",
};

const { name, age, notFound } = person;

console.log("name", name);
console.log("age", age);
console.log("natFound", notFound);
